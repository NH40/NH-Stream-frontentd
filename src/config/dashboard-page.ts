class DashboardPage {
  HOME = '/dashboard'

  SETTINGS = `${this.HOME}/settings`

  KEYS_STREAMS = `${this.HOME}/keys`

  CHAT = `${this.HOME}/chat`

  FOLLOWERS = `${this.HOME}/followers`

  SPONSORS = `${this.HOME}/sponsors`

  PLANS = `${this.HOME}/plans`

  TRANSACTIONS = `${this.HOME}/transactions`
}

export const DASHBOARD_PAGE = new DashboardPage()
